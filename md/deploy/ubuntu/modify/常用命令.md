tar -czf upload.tar.gz -C /data/toonflow uploads
tar -xzf upload.tar.gz -C /data/toonflow


# 1. 设置 yarn 镜像源为淘宝
yarn config set registry https://registry.npmmirror.com

# 2. 清除缓存
yarn cache clean
# rm -rf /usr/local/share/.cache/yarn

# 3. 强制重新下载
yarn install --force