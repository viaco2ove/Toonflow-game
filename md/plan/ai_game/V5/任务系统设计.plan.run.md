# 任务系统设计.plan.run.md

> 当前文件用于补充 plan.md 的实际执行记录和设计调整。plan.md 已上锁（`@no_modify`），如有改动请在本文件记录。

---

## T3 设计修正（2025-01-20）

### 重大修正

用户要求重新设计 T3：

1. **不实现** — 只改 plan.md
2. **plan.md 已上锁但要求修改** — 用户明确要求改 plan.md
3. **T3 重新设计为：增加 ai_agent:意图分析师，只对接 Qwen3-0.6B**

### 修正后的 T3 设计

plan.md 9.3 节已重写为：

```
### 9.3 第三阶段：ai_agent:意图分析师（2 天）

- T3.1: Qwen3-0.6B 分类器（只对接 Qwen3-0.6B）
  - 文件: src/modules/game-runtime/agents/intentAnalyzer/QwenIntentClassifier.ts
  - 生成式分类，输出 JSON 格式
  - 置信度 < 0.7 → normal_dialog 兜底

- T3.2: 意图分析师 Agent 主入口
  - 命令未命中 → analyzeIntentWithAi → Qwen3-0.6B → normal_dialog 兜底
  - 置信度 < 0.7 自动降级

- T3.3: 接入 SessionService.addMessage 流程
  - 在 maybeResolve 之前调 analyzeIntentWithAi
  - 根据意图分流到对应处理器

- T3.4: 集成测试与监控埋点
```

### 设计依据

- [意图分析师.md](../../curr_design/游玩业务_ai_agent/意图分析师.md) L16-20：只对接 Qwen3-0.6B
- [ai_agent_工作流.md](../../curr_design/游玩业务_ai_agent/ai_agent_工作流.md)：意图分类 + 分流调度
- [意图分析师_prompt.md](../../curr_design/游玩业务_ai_agent/意图分析师_prompt.md)：system prompt 模板

### 不再实现的内容

以下内容已从 T3 设计中移除：
- m3e-small 嵌入分类器（moka-ai/m3e-small 是嵌入模型，不适合生成分类）
- Qwen2.5-1.5B / 3B 降级链（暂时只接 Qwen3-0.6B）

### 供应商映射说明

**意图分析师.md 中的供应商名称**（用户面向）vs **代码中的 manufacturer key**：

| 意图分析师.md | 代码 manufacturer key | 说明 |
|-------------|---------------------|------|
| local文本模型 | `autodl_chat` / `autodl` | 本地模型，需配置 baseURL |
| 火山引擎 | `doubao` / `volcengine` | 豆包系列 |
| DeepSeek | `deepseek` | deepseek-chat / deepseek-reasoner |
| OpenAI | `openai` | gpt-4o / gpt-4o-mini |
| Gemini | `gemini` | gemini-2.5-pro / gemini-2.5-flash |
| t8star | `t8star` | gpt-5.4-pro / gemini-2.5-pro |

**注意**：
1. `local文本模型` 对接的是 autodl_chat/autodl（OpenAI-Compatible），Qwen3-0.6B 需要通过 Ollama 或 LM Studio 暴露为 OpenAI-Compatible 接口
2. 代码中使用 `getPromptAi("intentClassifierModel", userId)` 获取用户配置，对应 `t_config` 表中的配置
- 预定义意图向量库

---

## 执行记录

| 日期 | 完成内容 | 备注 |
|------|----------|------|
| 2025-01-20 | Phase 1 (T1.1-T1.5) | 命令快路径完成 |
| 2025-01-20 | Phase 2 (T2.1-T2.3) | 移除自由章节限制 + taskList |
| 2025-01-20 | T3 重新设计 | 修正为 ai_agent:意图分析师，只对接 Qwen3-0.6B |