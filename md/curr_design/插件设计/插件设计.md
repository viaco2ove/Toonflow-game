# Toonflow-Game 插件系统设计方案

> 参考 Tavo 插件架构，为 Toonflow-Game 设计的插件系统方案

## 一、设计目标

| 目标 | 说明 |
|------|------|
| **小游戏扩展** | 为用户提供更多可嵌入的互动小游戏 |
| **UI 增强** | 面板、侧边栏、悬浮按钮等 UI 扩展 |
| **AI 能力增强** | 记忆管理、事件编排、角色状态等 Agent 能力 |
| **用户隔离** | 插件安装与用户绑定，保障隐私与安全 |

## 二、插件包格式 (.tpg)

沿用 Tavo 的 `.tpg` 格式（本质是 zip 压缩包），保持与 Tavo 生态的兼容性。

### 2.1 插件目录结构

```
toonflow-field-survival/     # 插件源码目录（最终打包为 .tpg）
├── manifest.json            # 插件元数据（必须）
├── entry.js                 # 前端入口脚本（minigame 插件必须）
├── entry.ts                 # 后端 TypeScript 入口（插件小游戏必须，编译为 entry.js）
├── ui/                      # 前端 UI 资源
│   ├── game.html           # 小游戏 HTML（iframe 加载）
│   ├── styles.css          # 样式文件
│   └── assets/             # 静态资源
├── vue/                     # Vue 组件目录（可选，用于 Vue 改造后的游戏 UI）
├── locales/                 # 国际化资源
│   ├── zh-CN.json
│   └── en.json
├── cover.png                # 插件封面（可选）
└── README.md                # 插件说明（可选）
```

### 2.2 manifest.json 规范

```json
{
  "id": "com.toonflow.minigame-field-survival",
  "name": "野外生存",
  "version": "1.0.0",
  "author": "Toonflow Team",
  "description": "在荒野中生存，寻找食物和水源，抵御野兽袭击，挑战极限生存天数。",
  "main": "entry.ts",
  "contributes": {
    "minigame": {
      "type": "field_survival",
      "title": "野外生存",
      "width": 480,
      "height": 640,
      "entry": "ui/game.html"
    },
    "sidebar": [
      {
        "id": "field-survival-cmd",
        "label": "野外生存",
        "command": "#野外生存"
      }
    ]
  }
}
```

**字段说明：**

| 字段 | 必须 | 说明 |
|------|------|------|
| `id` | ✅ | 反向域名格式唯一标识 |
| `name` | ✅ | 插件显示名称 |
| `version` | ✅ | 语义化版本 |
| `contributes.minigame` | 小游戏插件必须 | 小游戏配置 |
| `contributes.minigame.type` | ✅ | 小游戏类型标识（如 `field_survival`），前端 iframe 按此匹配 |
| `contributes.minigame.entry` | ✅ | iframe 加载的 HTML 入口（相对插件根目录） |
| `contributes.sidebar` | 推荐 | 侧边栏命令，`command` 字段（以 `#` 开头）会被后端扫描注册 |
| `main` | 小游戏插件必须 | 后端入口文件（`entry.ts`） |

> ⚠️ **已废弃字段**：`backendEntry`（原 Python 后端入口）、`specVersion`、`permissions`、`panel`、`settings`、`dependencies`，Toonflow 当前实现中不再使用。

## 三、系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     前端 (Toonflow-game-web)                     │
├─────────────────────────────────────────────────────────────────┤
│  插件管理界面                                                    │
│  ├─ SettingsPluginManagerDialog（上传/启停/卸载）                 │
│  └─ SceneSettings → "插件管理"入口                              │
│                                                                  │
│  插件运行时                                                      │
│  ├─ usePluginRuntime: 拉取插件清单 + 派生 sidebar/minigame 数据  │
│  ├─ ScenePlay: 命令面板注入插件命令                             │
│  └─ iframe panel: 插件小游戏 UI 容器（pluginMinigameView）       │
└─────────────────────────────────────────────────────────────────┘
                              ↕ REST API
┌─────────────────────────────────────────────────────────────────┐
│                      后端 (toonflow-game-app)                    │
├─────────────────────────────────────────────────────────────────┤
│  插件管理器（pluginRegistry.ts + plugin/ 路由）                  │
│  ├─ /plugin/list        → 列出用户已安装插件                    │
│  ├─ /plugin/install     → 上传安装 .tpg                         │
│  ├─ /plugin/uninstall  → 卸载插件                              │
│  ├─ /plugin/setEnabled → 启用/禁用                              │
│  └─ /plugin/getAsset   → 读取插件静态资源（JWT 鉴权）           │
│                                                                  │
│  插件执行器（PluginExecutor.ts）                                 │
│  ├─ scanPluginCommands(userId)   → 扫描已启用插件 sidebar.command │
│  ├─ findPluginByCommand(cmd)     → 命令 → 插件上下文映射         │
│  └─ executePluginAction(ctx, action, params, state)              │
│      └→ loadEntryModule(pluginDir) → dynamic import(entry.js)    │
│                                                                  │
│  小游戏控制器（MiniGameController.ts）                           │
│  ├─ detectGameTrigger → 先扫 RULEBOOKs，未命中则 scanPluginCommands │
│  ├─ RULEBOOKS.plugin  → 通杀 rulebook，从 manifest 取插件信息     │
│  └─ PluginExecutor    → 调用 entry.ts · handle_action           │
│                                                                  │
│  插件存储                                                        │
│  └─ t_plugin 表（用户隔离）+ <数据根>/Toonflow-game/plugins/<userId>/ │
└─────────────────────────────────────────────────────────────────┘
```

## 四、数据库设计

### 4.1 实际实现：t_plugin 单表

> ⚠️ **实现说明**：实际落地时合并为单表 `t_plugin`（见 `src/migrations/20260921_000001_add_plugin_table.js`），不再使用原方案的三表结构。

```sql
CREATE TABLE t_plugin (
  id            INTEGER PRIMARY KEY,
  userId        INTEGER NOT NULL,           -- 用户隔离（每用户一套安装）
  pluginId      TEXT NOT NULL,              -- manifest.id
  name          TEXT,                       -- manifest.name（fallback 字符串）
  version       TEXT,                       -- manifest.version
  author        TEXT,
  description   TEXT,
  dir           TEXT NOT NULL,              -- 插件文件目录名
  entry         TEXT,                       -- 前端入口（manifest.entry）
  backendEntry  TEXT,                       -- 后端入口（manifest.backendEntry，可空）
  manifestJson  TEXT NOT NULL,              -- 完整 manifest JSON 快照
  enabled       INTEGER DEFAULT 1,          -- 1 启用 / 0 禁用
  status        TEXT DEFAULT 'installed',   -- installed | disabled | error
  installedAt   BIGINT,                     -- 安装时间 ms
  updatedAt     BIGINT                      -- 更新时间 ms
);
CREATE UNIQUE INDEX idx_plugin_user_plugin ON t_plugin (userId, pluginId);
```

**字段说明：**

| 字段 | 说明 |
|------|------|
| `userId` | 用户隔离，不同用户安装同一插件各有一行 |
| `pluginId` | manifest 中的插件唯一标识 |
| `dir` | 插件文件实际存放目录（相对于插件根目录） |
| `manifestJson` | manifest 完整快照，供 `PluginExecutor.scanPluginCommands` 直接解析 |
| `enabled` | 运行时启用标志，扫描命令时只取 `enabled=1 && status='installed'` 的记录 |
| `status` | 生命周期状态 |

**插件文件存储目录：**

```
<数据根>/Toonflow-game/plugins/
└── <userId>/
    └── <pluginId>/
        ├── manifest.json
        ├── entry.js          ← tsc 编译产物
        ├── entry.ts          ← 插件源码（TypeScript）
        ├── ui/
        │   └── game.html     ← iframe 入口
        ├── vue/              ← Vue 改造目录（可选）
        └── locales/
            └── zh-CN.json
```

- 用户隔离：每用户独立目录
- 安装时先解压到 `.tmp/<timestamp>-<uuid>/`，定位 manifest 所在目录，再原子重命名到目标位置
- 卸载时先改名（避免 Windows 文件锁），再删除目录

## 五、后端 API 设计

### 5.1 插件管理 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/plugins/list` | GET | 获取用户已安装插件列表 |
| `/api/plugins/store` | GET | 获取插件商店（可选） |
| `/api/plugins/upload` | POST | 上传安装插件 |
| `/api/plugins/:pluginId/install` | POST | 安装插件到用户 |
| `/api/plugins/:pluginId/uninstall` | DELETE | 卸载用户插件 |
| `/api/plugins/:pluginId/enable` | POST | 启用插件 |
| `/api/plugins/:pluginId/disable` | POST | 禁用插件 |
| `/api/plugins/:pluginId/settings` | GET/PUT | 读取/保存插件设置 |

### 5.2 插件执行 API

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/plugins/:pluginId/execute` | POST | 执行后端插件逻辑 |
| `/api/plugins/:pluginId/state` | GET | 获取插件状态 |

### 5.3 API 响应格式

```typescript
// 统一响应格式
interface ApiResponse<T> {
  code: number;       // 0=成功, 其他=错误码
  message: string;
  data?: T;
}

// 插件列表响应
interface PluginListResponse {
  plugins: Array<{
    id: string;
    name: string;
    version: string;
    description: string;
    enabled: boolean;
    settings: Record<string, any>;
    isBuiltIn: boolean;
    coverUrl?: string;
  }>;
}

// 安装插件请求
interface InstallPluginRequest {
  pluginId: string;
  version?: string;  // 默认安装最新版本
}
```

## 六、前端插件运行时

### 6.1 PluginLoader

```typescript
class PluginLoader {
  private loadedPlugins: Map<string, PluginInstance> = new Map();

  // 加载插件
  async loadPlugin(plugin: PluginMeta): Promise<PluginInstance> {
    // 1. 获取插件文件列表
    // 2. 加载 entry.js
    // 3. 创建沙箱上下文
    // 4. 执行入口函数
    // 5. 注册 UI 组件
  }

  // 卸载插件
  unloadPlugin(pluginId: string): void {
    const instance = this.loadedPlugins.get(pluginId);
    if (instance?.cleanup) {
      instance.cleanup();
    }
    this.loadedPlugins.delete(pluginId);
  }
}
```

### 6.2 插件 API 上下文

```typescript
// 插件获得的受限 API
interface PluginContext {
  // 状态读写
  getState(key: string): Promise<any>;
  setState(key: string, value: any): Promise<void>;

  // AI 生成
  generate(prompt: string, options?: GenerateOptions): Promise<string>;

  // 用户交互
  showPanel(config: PanelConfig): void;
  hidePanel(): void;
  toast(message: string, type?: 'info' | 'success' | 'error'): void;

  // 设置
  getSetting(key: string): any;
  setSetting(key: string, value: any): Promise<void>;

  // 游戏控制
  triggerEvent(eventName: string, data?: any): void;
  onEvent(eventName: string, handler: EventHandler): void;

  // 资源
  getAssets(path: string): string;  // 获取资源 URL
}

// 插件权限验证
const permissionMap = {
  'ui': ['showPanel', 'hidePanel', 'toast'],
  'game_state': ['getState', 'setState', 'triggerEvent', 'onEvent'],
  'ai_generate': ['generate'],
  'voice': ['playVoice', 'stopVoice'],
  'assets': ['getAssets']
};
```

### 6.3 插件 UI 挂载

```typescript
// 在聊天界面中挂载插件面板
function mountPluginUI(fragment: HtmlFragment, mountPoint: string) {
  // mountPoint 格式: "/chat/body/end" 或自定义选择器
  const container = document.querySelector(mountPoint);
  if (container) {
    container.appendChild(createFragment(fragment));
  }
}

// 示例：侧边栏按钮注册
function registerSidebarButton(config: SidebarConfig) {
  const sidebar = document.querySelector('.sidebar-actions');
  if (sidebar) {
    const btn = createButton(config);
    btn.onclick = () => executePluginAction(config.id);
    sidebar.appendChild(btn);
  }
}
```

## 七、MiniGame 插件示例

### 7.1 猜数字小游戏 manifest.json

```json
{
  "specVersion": 1,
  "id": "com.toonflow.minigame-guess-number",
  "name": {
    "fallback": "猜数字"
  },
  "version": "1.0.0",
  "author": "Toonflow",
  "description": {
    "fallback": "经典的猜数字小游戏，AI 会想一个 1-100 的数字，你来猜！"
  },
  "permissions": ["ui", "game_state"],
  "contributes": {
    "minigame": {
      "type": "guess_number",
      "title": "猜数字",
      "width": 320,
      "height": 400
    },
    "sidebar": [
      {
        "id": "guess-number-start",
        "label": "猜数字",
        "icon": "gamepad"
      }
    ]
  }
}
```

### 7.2 entry.js 示例

```javascript
// 猜数字游戏 - entry.js
class GuessNumberGame {
  constructor(ctx) {
    this.ctx = ctx;
    this.state = {
      target: Math.floor(Math.random() * 100) + 1,
      attempts: 0,
      maxAttempts: 10,
      gameOver: false,
      won: false
    };
  }

  // 渲染游戏界面
  render() {
    return `
      <div class="guess-game">
        <h3>🎯 猜数字 (1-100)</h3>
        <p>剩余次数: ${this.state.maxAttempts - this.state.attempts}</p>
        <input type="number" id="guess-input" min="1" max="100" />
        <button onclick="guessNumber(this.ctx)">猜！</button>
        <div id="guess-result"></div>
        <button onclick="this.ctx.hidePanel()">关闭</button>
      </div>
    `;
  }

  // 处理猜测
  guess(number) {
    this.state.attempts++;

    if (number === this.state.target) {
      this.state.won = true;
      this.state.gameOver = true;
      return { result: 'correct', message: `🎉 猜对了！答案就是 ${number}` };
    } else if (number < this.state.target) {
      return { result: 'hint', message: '📈 太小了，再试试！' };
    } else {
      return { result: 'hint', message: '📉 太大了！' };
    }
  }

  cleanup() {
    // 清理资源
  }
}

// 全局工厂函数
function createPlugin(ctx) {
  return new GuessNumberGame(ctx);
}
```

## 八、后端 MiniGameAgent

### 8.1 MiniGameAgent 设计

```python
# modules/agents/minigame/MiniGameAgent.py

class MiniGameAgent:
    """
    小游戏 AI Agent
    负责执行后端小游戏逻辑，与前端插件协同工作
    """

    def __init__(self, plugin_id: str, plugin_entry: str):
        self.plugin_id = plugin_id
        self.plugin_entry = plugin_entry  # entry.py 路径
        self.game_states: Dict[str, dict] = {}  # session_id -> game_state

    async def execute(self, action: str, params: dict, context: dict) -> dict:
        """执行游戏动作"""
        # 加载插件
        module = importlib.import_module(self.plugin_entry.replace('.py', '').replace('/', '.'))

        # 获取或创建游戏实例
        session_id = context.get('session_id')
        if session_id not in self.game_states:
            self.game_states[session_id] = {}

        # 调用插件逻辑
        if hasattr(module, 'handle_action'):
            return await module.handle_action(
                action=action,
                params=params,
                state=self.game_states[session_id],
                context=context
            )
        else:
            raise ValueError(f"Plugin {self.plugin_id} missing handle_action")
```

### 8.2 API 调用流程

```
前端插件                    后端 Agent                      游戏逻辑
   │                           │                               │
   │  triggerEvent('guess')    │                               │
   │  ──────────────────────>  │                               │
   │                           │  execute('guess', {num: 50})   │
   │                           │  ────────────────────────────> │
   │                           │                               │
   │                           │         {result: 'hint', ...} │
   │                           │  <──────────────────────────── │
   │                           │                               │
   │  {result: 'hint', ...}   │                               │
   │  <────────────────────── │                               │
   │                           │                               │
```

## 九、插件安全模型

### 9.1 沙箱隔离

| 限制项 | 说明 |
|--------|------|
| **网络访问** | 禁止直接网络请求，必须通过 `ctx.fetch()` |
| **存储访问** | 仅允许读写插件专属的 storage |
| **文件访问** | 禁止访问文件系统 |
| **代码执行** | 前端使用 iframe 沙箱，后端使用 subprocess 隔离 |

### 9.2 权限审批

```json
{
  "permissions": {
    "ui": { "level": "allow" },
    "game_state": { "level": "prompt" },  // 需要用户确认
    "ai_generate": { "level": "prompt", "maxTokens": 500 },
    "network": { "level": "deny" }
  }
}
```

## 十、插件生命周期

```
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│  上传   │ ──> │  安装   │ ──> │  启用   │ ──> │  运行   │
└─────────┘     └─────────┘     └─────────┘     └─────────┘
                                                      │
                                                      ▼
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│  删除   │ <── │  禁用   │ <── │  卸载   │ <── │  错误   │
└─────────┘     └─────────┘     └─────────┘     └─────────┘
```

### 10.1 状态流转

| 状态 | 说明 |
|------|------|
| `uploaded` | 已上传，待安装 |
| `installed` | 已安装到用户，待启用 |
| `enabled` | 已启用，运行中 |
| `disabled` | 已禁用，不运行 |
| `error` | 运行出错 |

## 十一、实现优先级

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P0 | 基础框架 | 插件上传/安装/启用/禁用 |
| P0 | 前端加载器 | JS 动态加载与执行 |
| P1 | 插件管理界面 | 列表、设置、开关 |
| P1 | 小游戏支持 | minigame 类型扩展 |
| P2 | 后端 Agent | Python 插件执行 |
| P2 | 插件商店 | 社区分享 |
| P3 | 权限系统 | 沙箱与审批 |

## 十二、文件结构

```
toonflow-game-app/
├── src/
│   ├── modules/
│   │   ├── plugin-manager/           # 插件管理模块
│   │   │   ├── PluginService.ts      # 插件服务
│   │   │   ├── PluginLoader.ts        # 插件加载器
│   │   │   ├── PluginSandbox.ts       # 沙箱隔离
│   │   │   └── types.ts               # 类型定义
│   │   └── agents/
│   │       └── minigame/              # 小游戏 Agent
│   │           ├── MiniGameAgent.ts
│   │           └── handlers/           # 内置小游戏
│   │               ├── guess_number.py
│   │               └── ...
│   ├── routes/
│   │   └── plugin/                    # 插件 API 路由
│   │       ├── index.ts
│   │       ├── list.ts
│   │       ├── install.ts
│   │       └── execute.ts
│   └── lib/
│       └── initDB.ts                   # 数据库表定义
│
├── Toonflow-game-web/
│   └── src/
│       ├── components/
│       │   └── PluginManager/         # 插件管理组件
│       │       ├── PluginList.vue
│       │       ├── PluginStore.vue
│       │       └── PluginSettings.vue
│       ├── plugins/                   # 插件运行时
│       │   ├── PluginRuntime.ts
│       │   ├── PluginContext.ts
│       │   └── PluginBridge.ts
│       └── stores/
│           └── pluginStore.ts
│
└── toonflow-game-plugins/             # 内置插件源码
    ├── system/                         # 系统级插件
    └── minigame/                      # 内置小游戏
```

## 附录 A: Tavo vs Toonflow 插件对比

| 特性 | Tavo 插件 | Toonflow 插件 |
|------|----------|--------------|
| 格式 | .tpg (zip) | .tpg (zip) |
| 前端入口 | entry.js | entry.js |
| 后端入口 | - | entry.py |
| UI 挂载 | htmlFragments | panel + minigame |
| 权限模型 | 固定权限列表 | 可配置权限 |
| 用户隔离 | Tavo 内置 | 显式用户绑定 |
| AI 能力 | 依赖 App | MiniGameAgent |

## 附录 B: manifest.json 完整字段

```typescript
interface PluginManifest {
  specVersion: number;           // 规范版本
  id: string;                    // 唯一标识 (反向域名)
  name: LocalizedString | string;
  version: string;               // 语义化版本
  author?: string;
  description: LocalizedString | string;
  minAppVersion?: string;        // 最低 App 版本
  entry?: string;                // 前端入口 (默认 entry.js)
  backendEntry?: string;         // 后端入口 (默认 entry.py)
  cover?: string;                // 封面图路径

  permissions: Permission[];     // 权限列表
  contributes: {
    panel?: PanelConfig;         // 面板配置
    sidebar?: SidebarItem[];      // 侧边栏按钮
    settings?: SettingsSchema;   // 设置定义
    minigame?: MinigameConfig;   // 小游戏配置
  };

  dependencies?: Record<string, string>;  // 依赖插件
}

type Permission = 'ui' | 'game_state' | 'ai_generate' | 'voice' | 'assets' | 'network';

interface LocalizedString {
  '$t': string;           // i18n key
  'fallback'?: string;    // 回退文本
}
```
