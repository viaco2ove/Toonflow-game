# v1.0.2
## 我们思考一个故事版本升级问题。 已经有人游玩了。 然后作者又去编辑故事。会怎么样
contentVersion 字段的作用
会话创建时会记录 contentVersion（基于世界+章节内容 hash），初始快照也存了同样的版本号。

这是个架构设计问题，让我先看看当前数据模型怎么关联故事和会话。
数据库里有 `contentVersion` 字段。让我看看完整逻辑：
搞清楚了。当前系统的版本机制：

**`contentVersion` 字段的作用**

会话创建时会记录 `contentVersion`（基于世界+章节内容 hash），初始快照也存了同样的版本号。

**作者编辑故事后的实际影响**

| 编辑类型 | 会话已存在 | 会话未开始 |
|---------|-----------|-----------|
| 章节文本改了 | 旧 session 继续用旧 state，**不受影响** | 新 session 用新版本 |
| 世界设定改了 | 旧 session 继续用旧 state，**不受影响** | 新 session 用新版本 |
| 预热快照缓存 | 缓存 version 变了，**自动失效**，重新生成 | 正常 |

**关键点**：
- 会话的 `stateJson` 是独立的快照，编辑故事**不会修改**已有会话
- 章节预热快照（存在世界 `settings` 里）会在作者保存时**自动失效**（version hash 变了）
- 用户继续玩旧 session 时
SessionService.ts:3857-3859 每轮编排都 db("t_storyChapter").where({id}) 实时拉最新内容；stateJson 里不含 chapter.content

**但有问题的地方**：
1. 已有 session 继续玩时，章节内容
SessionService.ts:3857-3859 每轮编排都 db("t_storyChapter").where({id}) 实时拉最新内容；stateJson 里不含 chapter.content
所以章节一直都说最新版。但是有可能一些动态数据忽略了改动沿用旧的设计。
例如认为性格什么的。
3. `contentVersion` 只记录在 session 表，没看到有**版本冲突检测**或**提示用户版本已更新**的逻辑

** 考虑什么场景？** 比如：
- 用户玩到一半，作者发了新版，能否提示用户？
- 旧 session 能否"升级"到新版同时保留进度？
- 还是有具体的 bug 场景？

## 也就是故事发布没有版本号信息？有没有发布日期信息？contentVersion  是发布故事时的故事数据， 还是游玩时的存档数据
让我查清楚数据模型。
数据库字段汇总：

| 表 | 字段 | 说明 |
|---|---|---|
| `t_storyWorld` | `publishStatus` | published / draft |
| `t_storyWorld` | `updateTime` | 最后修改时间 |
| `t_storyChapter` | `updateTime` | 最后修改时间 |
| `t_gameSession` | `contentVersion` | session 创建时的 world+chapter hash |

**结论**：
- **没有版本号**，只有 `updateTime`（最后修改时间戳）
- **没有发布日期**
- `contentVersion` 是**游玩时**的快照 hash，记录的是"session 创建那一刻的故事版本"

所以系统目前是**单向追踪**：只能知道"这个 session 是什么时候、用什么版本创建的"，但没有"故事第 N 版"的概念。作者发了几个版本、什么时候发的，**都不记录**。

当前隐患
其实比"没有版本号"更危险的是：章节内容是运行时从 t_storyChapter 实时读取的，不是 session 快照里的。所以作者改了章节内容，旧 session 的编排师/发言器会读到新内容，但 session 的进度状态（phaseId、eventIndex）还是旧的。如果作者删了一个 ## 标题或改了事件结构，进度状态就对不上了--静默错位。

当前系统没有草稿表/发布表分离，就一张表，用状态字段区分：

t_storyWorld
  publishStatus: "draft" | "publishing" | "published" | "publish_failed"
settings 里也存了一份 publishStatus（冗余校验用的）。

发布时发生的事（publishWorldSynchronously）：

状态改成 publishing
为所有章节预先生成开局快照（存到 world.settings[chapterInitialSnapshots]）
状态改成 published
没有复制表、没有版本记录。草稿和发布是同一个表的同一个行，只是状态不同。

数据关系图：

t_storyWorld ────── t_storyChapter (1:N)
    │                    │
    ├── publishStatus ───┤
    ├── settings ────────┤  ← 预热快照存这里
    └── updateTime ──────┘

t_gameSession ────── t_storyWorld (N:1)
    │
    ├── contentVersion = hash(world + chapter 创建时)
    └── stateJson = session 独立运行态（不引用 world/chapter 原文）

contentVersion 字段存在但 hash 输入是 14 个字段
（sessionInitialSnapshot.ts:58-82）；运行时根本不读它，
仅 listSession.ts:157 返回给前端展示

## 当前版本的优缺点
简单，实际游玩，其实没有太大的问题。


# 下个版本的设计。方向1
**重要程度** 中
**风险程度** 中
**改动范围程度** 高
**功能收益程度** 中

第一步：版本标记 + 检测（最小改动）
```
t_storyWorld:
  + publishedVersion  INTEGER   -- 每次发布 +1
  + publishedAt       INTEGER   -- 发布时间戳

t_gameSession:
  + worldVersion      INTEGER   -- session 创建时的 publishedVersion
```
用户打开旧 session 时，对比 session.worldVersion 和 world.publishedVersion：

- 相同 → 正常继续
- 不同 → 前端显示"故事已更新到第 N 版"，让用户选择"继续旧版"或"重新开始"
不需要版本历史表，只记当前版本号就够了。作者点"发布"时 publishedVersion++。


## 第二步 故事发布的独立表和数据
t_storyWorld_published
storyWorld_published ，t_storyWorld-》t_storyWorld_published -》t_gameSession
t_gameSession 不需要记录章节内容。t_storyWorld_published 记录就行

明白了，思路更干净。整理一下你的设计：

```
作者编辑区                          玩家游玩区
┌──────────────┐    发布           ┌────────────────────┬─────────────────────┐
│ t_storyWorld │ ──────────────►   │ t_storyWorld       │ t_storyWorld        │
│ (草稿)       │                   │ _published         │ _published          │
│              │                   │  v1                │  v2                 │
├──────────────┤                   ├────────────────────┼─────────────────────┤
│ t_storyChapter│ ──────────────►  │ t_storyChapter     │ t_storyChapter      │
│ (草稿)       │                   │ _published (v1)    │ _published (v2)     │
└──────────────┘                   └─────────┬──────────┴─────────┬───────────┘
                                             │                    │
                                             └────────┬───────────┘
                                                      │
                                              ┌───────▼────────┐
                                              │ t_gameSession  │
                                              │ worldPublishId │ ← 关联到具体版本
                                              │ stateJson      │ ← 只存运行态，不存章节内容
                                              └────────────────┘
```
### 数据结构问题。
最好是压缩的格式。bin 或json。 减少存储空间。
content         BLOB      -- 压缩后故事数据。
全部数据塞到content    包括章节和角色统统传入content 。

content 的数据量问题。
章节是否独立到t_storyChapter_published 表？
一张表 + 一个 BLOB，最简方案。推荐。

表结构：

t_storyWorld_published
───────────────────────────────
  id            INTEGER PRIMARY KEY
  worldId       INTEGER   -- 原始世界ID
  version       INTEGER   -- 发布版本号
  content       BLOB      -- gzip(JSON: world + chapters + characters + ...)
  publishedAt   INTEGER   -- 发布时间戳
  publishedBy   TEXT      -- 发布者（可选）
───────────────────────────────
  UNIQUE(worldId, version)
  INDEX(worldId)
content 解压后的 JSON 结构：

json
{
  "world": { "title": "...", "settings": {...}, "background": "..." },
  "chapters": [
    { "id": 1, "title": "...", "content": "...", "runtimeOutline": "...", "sort": 1 },
    ...
  ],
  "characters": [ ... ],
  "tasks": [ ... ],
  "triggers": [ ... ]
}
一条 row = 一个完整可游玩的故事快照。t_gameSession 只需关联 publishId，运行时解压一次缓存到内存就行。

**核心逻辑**：
- 发布 = 把草稿表数据复制到 `_published` 表，生成新版本
- 运行时 = 从 `_published` 表读章节内容，不从草稿表读
- Session = 关联 `t_storyWorld_published.id`，作者编辑草稿完全不影响在运行的 session

几个需要确认的设计点：

1. **每次发布新增行还是覆盖？** 新增行可以保留历史版本（老 session 继续用旧版本），覆盖则老 session 直接断裂。
新增行
删除故事时全部发布数据行清除。

2. **`t_storyChapter_published` 也要对应建一张？** 因为章节是世界的一对多，published 表也要同步。
数据结构问题。最好是压缩的格式。bin 或json。 减少存储空间。
t_storyChapter_published
─────────────────────────────────────────────────────────
  id              INTEGER PRIMARY KEY
  worldPublishId  INTEGER   -- 关联 t_storyWorld_published
  chapterId       INTEGER   -- 原始草稿 chapterId（不变，用于映射）
  version         INTEGER   -- 发布版本号

  content         BLOB      -- 压缩后的章节全文（gzip）
  runtimeOutline  TEXT      -- 解析后的大纲（已结构化，不大）
  title           TEXT      -- 标题（不压缩，直接读）

  publishedAt     INTEGER   -- 发布时间戳

  UNIQUE(worldPublishId, chapterId, version)
  INDEX(worldPublishId)
  INDEX(chapterId)          -- 迁移时快速定位旧 chapterId

暂时不独立，先用一个 BLOB 存到 t_storyWorld_published.content 里。



3. **老 session 的处理**：发布新版后，旧 session 关联的还是旧 `published.id`，继续游玩没问题。前端检测到有新版本时提示用户"故事已更新，是否重新开始"。
直接在“聊过”面板 的对应“故事块” 的“继续”按钮左边 显示：“故事已更新”按钮，点击后 弹框提示：“是否确定升级故事，有可能导致存档数据丢失！”

弹框还有进度处理选项
- 保留不处理
效果跟v1.0.2 基本差不多。
- 程序处理
- ai处理

- 不保留存档进度的处理？
无需处理，用户自己可以删掉的。 本来就有的功能。

## 存档进度迁移（复杂）
如果用户想"升级到新版同时保留存档进度"：
对比新旧章节的 phase 结构
如果结构没变（只改了文本）→ 直接用新章节内容 + 旧进度
如果结构变了 → 提示用户"改动较大，建议重新开始

升级故事自动保留旧存档进度。
存档进度迁移？怎么迁移？
最简单的是不是完全不改旧存档进度数据。
复杂一点就是硬编码调整？
最复杂一点就是 ai agent : 存档进度迁移器？
直接扔数据进去解析返回？

对，就这三个层次。让我先确认一下进度状态到底存了什么：
进度状态的核心字段：

```ts
ChapterProgressState {
  chapterId: number      // 章节ID（可能在新版里变了）
  phaseId: string        // 当前阶段ID（可能在新版里变了）
  phaseIndex: number     // 阶段索引
  stageIndex: number     // stage 索引
  eventIndex: number     // 事件索引（可能在新版里变了）
  eventSummary: string   // 事件摘要文本（可能对不上了）
  eventStatus: string    // idle/active/waiting_input/completed
  completedEvents: string[]  // 已完成事件列表
  ...
}
```

三个方案对比：

|      | 不改                              | 硬编码                       | AI Agent                      |
|------|---------------------------------|---------------------------|-------------------------------|
| **原理** | 旧 session 继续用旧 published 版本，不迁移 | 发布新版时写迁移规则（如 phaseId 映射表） | 新版 vs 旧版内容扔给 AI，AI 输出进度 patch |
| **复杂度** | 无                               | 中等                        | 高                             |
| **准确性** | ✅ 完美保留                          | ⚠️ 只能处理预期内改动              | ⚠️ AI 可能推断错误                  |
| **适用场景** | 用户不想出现改动，虽然不太适配故事新版本，但是也能玩      | 用户接受少量改动，中度适配故事的新版本       | 用户接受自己的存档进度出现大的改动， 高度适配故事的新版本 |

## 存档数据包括
对，我只考虑了章节进度，漏了运行时动态数据。让我看看 state 里还有什么：
完整的 session state 结构：

```
state {
  // 章节进度 —— 需要迁移
  chapterProgress {
    chapterId, phaseId, phaseIndex, eventIndex, eventSummary,
    eventStatus, userNodeId, userNodeIndex, userNodeStatus,
    completedEvents: string[]   // "phase:xxx" | "userNode:xxx"
  }
  
  // 当前事件 —— 从 chapterProgress 派生，迁移后自动同步
  currentEvent { index, kind, summary, status }
  
  // AI生成的事件动态状态 —— 需要迁移（更新 phaseId 引用）
  dynamicEvents: RuntimeDynamicEventState[] {
    eventIndex, phaseId, summary, runtimeFacts, memorySummary, ...
  }
  
  // 角色属性卡 —— 原样迁移
  player {}, narrator {}, npcs {}, inventory [], unlockedRoles []
  
  // 状态变量 —— 原样迁移
  flags {}, vars {}
  
  // 最近事件历史 —— 过滤已删除的 phaseId 引用
  recentEvents []
  
  // 小游戏状态 —— 原样迁移
  miniGame {}
  
  // 回合状态 —— 根据新 phase 调整
  turnState { canPlayerSpeak, expectedRole, lastSpeaker, ... }
}
```

完整迁移函数：

```ts
function migrateSessionProgress(
  oldState: SessionState,
  oldChapter: PublishedChapter,
  newChapter: PublishedChapter,
): { state: SessionState; migrationReport: MigrationReport } {
  const oldProgress = oldState.chapterProgress;
  
  // 1. 构建 phase 映射
  const oldPhases = extractPhases(oldChapter.content);  // ["决定", "出发", ...]
  const newPhases = extractPhases(newChapter.content);
  
  const phaseMapping = buildPhaseMapping(oldPhases, newPhases);
  // { "决定": "决定", "出发": "入校", deleted: ["旧删除了"] }
  
  // 2. 迁移 chapterProgress
  const newPhaseId = phaseMapping[oldProgress.phaseId] 
    ?? newPhases[oldProgress.phaseIndex] 
    ?? newPhases[0] 
    ?? oldProgress.phaseId;
  
  const migrationNotes: string[] = [];
  if (!phaseMapping[oldProgress.phaseId] && oldProgress.phaseId) {
    migrationNotes.push(`phase "${oldProgress.phaseId}" 被删除或重命名`);
  }
  
  const migratedProgress = {
    ...oldProgress,
    chapterId: newChapter.id,
    phaseId: newPhaseId,
    phaseIndex: newPhases.indexOf(newPhaseId),
    // completedEvents 里的 phaseId 也做映射
    completedEvents: oldProgress.completedEvents.map(e => {
      if (e.startsWith("phase:")) {
        const oldId = e.slice(6);
        const newId = phaseMapping[oldId] ?? oldId;
        return `phase:${newId}`;
      }
      return e;
    }),
  };
  
  // 3. 迁移 dynamicEvents（更新 phaseId 引用）
  const migratedDynamicEvents = oldState.dynamicEvents.map(ev => {
    if (ev.phaseId && phaseMapping[ev.phaseId]) {
      return { ...ev, phaseId: phaseMapping[ev.phaseId] };
    }
    return ev;
  });
  
  // 4. 过滤 recentEvents（保留引用仍然有效的）
  const validPhaseIds = new Set(newPhases);
  const migratedRecentEvents = oldState.recentEvents.filter(e => 
    !e.phaseId || validPhaseIds.has(e.phaseId)
  );
  
  // 5. 调整 turnState
  const newPhaseIndex = newPhases.indexOf(newPhaseId);
  const migratedTurnState = {
    ...oldState.turnState,
    canPlayerSpeak: true,
    expectedRoleType: "player",
    expectedRole: oldState.player?.name || "用户",
  };
  
  return {
    state: {
      ...oldState,
      chapterId: newChapter.id,
      chapterProgress: migratedProgress,
      dynamicEvents: migratedDynamicEvents,
      recentEvents: migratedRecentEvents,
      turnState: migratedTurnState,
    },
    migrationReport: {
      oldChapterId: oldChapter.id,
      newChapterId: newChapter.id,
      phaseMapping,
      notes: migrationNotes,
      success: true,
    },
  };
}
```

**迁移报告**（返回给前端展示）：

```ts
MigrationReport {
  oldChapterId / newChapterId
  phaseMapping: { [oldId]: newId }  // 映射关系
  notes: string[]   // "phase X 被删除，已定位到附近 phase"
  success: boolean  // 是否有未解决冲突
}
```

前端展示给用户："检测到故事已更新，是否升级？变更：章节新增 phase '入校'，删除 '苏醒'..."

## ai agent: 版本更新存档升级助手
[ai_agent_版本更新存档升级助手.md](../%E6%B8%B8%E7%8E%A9%E4%B8%9A%E5%8A%A1_ai_agent/ai_agent_%E7%89%88%E6%9C%AC%E6%9B%B4%E6%96%B0%E5%AD%98%E6%A1%A3%E5%8D%87%E7%BA%A7%E5%8A%A9%E6%89%8B.md)

## 问题点
BLOB 方案的真问题不是存储，而是读取路径：

原本：db("t_storyChapter").where({id}) 一次单行查询
改后：db("t_storyWorld_published").where({id}) 查整行 → 解压 gzip → JSON.parse → 在内存里按 chapterId 找
如果不做缓存层，每轮编排都解压一次，CPU 和内存都会爆。建议加一层 PublishedContentCache（session 级内存缓存）。

漏掉的关键场景：migrateSessionProgress 里说"角色属性卡、状态变量原样迁移"，但如果新版删除了某个 NPC 或改了某个变量名，原样迁移会产生悬空引用：

npcs 里有个 李四，新版章节里没有这个 NPC 了 → 编排师读到不存在的角色
flags.questA = true，新版触发的条件改成 questB → 触发器永远不触发

## 开发难度评估

| 阶段 | 改动范围 | 难度 | 工时估算 | 风险点 |
|------|---------|------|---------|--------|
| **第一步** 版本字段 + 检测 | initDB 加 3 字段、fixDB 迁移、`publishWorldSynchronously` 加 `publishedVersion++`、listSession 返回版本对比、前端加"故事已更新"提示 | 低 | 1-2 天 | 低 |
| **第二步** 独立发布表 + BLOB | 新建表、发布逻辑改造（草稿→压缩→写 _published）、**runtime 链路大改**（8+ 处 DB 查询改路径）、session 关联 `worldPublishId`、内存缓存层、删除级联 | 中-高 | 5-8 天 | runtime 链路遗漏某处没改 → 静默 bug |
| **第三步** 存档迁移（硬编码） | `migrateSessionProgress` 函数 + 前端迁移报告 UI + 悬空引用清理 | 中 | 3 天 | 中（phase 映射不全 → 进度错乱） |
| **第三步** AI Agent 迁移 | Agent prompt 设计 + 调用链 + 错误兜底 + 测试 | 高 | 7-10 天 | 高（AI 推断错误难复现） |

**总计**：完整三步 + AI Agent 迁移 ≈ 16-23 天；只做前两步 + 硬编码迁移 ≈ 9-13 天。


## 四、开发收益评估

| 收益项 | 价值 | 说明 |
|--------|------|------|
| 解决"作者编辑草稿静默影响在运行 session" | 🔴 高 | 这是当前架构的**真 bug**，会随作者增多而暴露 |
| 用户感知"故事已更新" | 🟡 中 | 体验改善，但非核心痛点 |
| 支持版本回滚（保留历史 published 行） | 🟡 中 | 方案没设计作者侧回滚 UI，潜在价值未兑现 |
| 存档迁移平滑升级 | 🟢 低 | "作者改已发布故事"是低频场景，迁移使用率低 |
| AI Agent 迁移 | 🟢 低 | ROI 极低，建议砍掉 |

## 总结

| 维度 | 评分 | 说明 |
|------|------|------|
| 方案合理性 | 7/10 | 方向对，但第一步不能独立上线；BLOB 方案需补缓存层；存档迁移漏悬空引用 |
| 开发难度 | 中-高 | 主要难点在第二步的 runtime 链路改造（8+ 处 DB 查询改路径） |
| 开发收益 | 中-高 | 核心收益在第二步（草稿/发布分离），第一步和第三步收益偏低 |
| 风险 | 中 | runtime 改造遗漏某处会静默 bug；存档迁移 phase 映射不全会进度错乱 |
| ROI（完整版 vs 精简版） | 精简版更优 | 砍掉 AI Agent 迁移，工时从 16-23 天降到 9-13 天，收益损失 <10% |

# 下个版本的设计。方向2

集中做"版本感知 + 发布门控的动态更新"。明确取舍：**忽略旧版本的存档稳定需求**——发布新版后，所有在运行的 session 统一切到新版，不再保留旧版让老存档继续玩。换来砍掉多版本累积、砍掉存档迁移、砍掉 BLOB 压缩，大幅降复杂度。

**重要程度** 中
**风险程度** 低-中
**改动范围程度** 中
**功能收益程度** 中

## 核心思路

沿用方向1 的"草稿/发布分离"，但只保留**单版本**：

- 作者在 `t_storyWorld` / `t_storyChapter`（草稿表）上编辑
- 点"发布"时，把草稿快照复制到 `t_storyWorld_published` / `t_storyChapter_published`（发布表），覆盖该故事的上一版
- **runtime 一律改读发布表**，不再实时读草稿表
- session 关联 `worldPublishId`，版本号 `worldVersion` 用于感知"故事已更新"

### 关键修正（相对方向2 初稿）：不砍 content

初稿"砍掉 content、只快照 runtimeOutline"有两个硬伤，必须推翻：

1. **结构与正文割裂**。`runtimeOutline` 本就是从 `content` 解析出来的。冻 outline、放 content，作者改了 `##` 标题顺序后，outline（冻在旧发布）还是旧序，content（live）已是新序，编排师按 outline 取错段落——比现状更别扭（现状至少 outline 与 content 同源一致）。
2. **"省存储"动机不成立**。BLOB/gzip 只在**多版本累积**时才必要。单版本覆盖下 content 不累积，直接当普通 TEXT 列存，查询成本和现状 `db("t_storyChapter").where({ id })` 相同，**不需要方向1 担心的内存缓存层**。砍 content 省下的那点存储，换来的是割裂 bug，不划算。

## 表结构

```
t_storyWorld_published
  id            INTEGER PRIMARY KEY
  worldId       INTEGER   -- 原始草稿世界ID
  version       INTEGER   -- 发布版本号（每次发布 +1，仅用于版本感知）
  publishedAt   INTEGER   -- 发布时间戳
  publishedBy   TEXT      -- 发布者（可选）
  -- world 本身字段（title / settings / playerRole / narratorRole / background 等）整行复制快照
  UNIQUE(worldId)          -- 单版本：一个 worldId 只对应一行
  INDEX(worldId)
```

world 字段也快照进 published（整行复制，不压缩）。理由：`world.settings` 里有预热快照、触发动效配置等运行态数据，不快照则作者改草稿期间运行中 session 仍受影响，违背"发布门控"。

```
t_storyChapter_published
  id              INTEGER PRIMARY KEY
  worldPublishId  INTEGER   -- 关联 t_storyWorld_published.id
  chapterId       INTEGER   -- 原始草稿 chapterId（不变，用于映射）
  title           TEXT      -- 发布时标题快照
  content         TEXT      -- 发布时正文快照（不压缩，直接存）
  runtimeOutline  TEXT      -- 发布时解析后的 phase 结构快照
  sort            INTEGER   -- 发布时章节排序
  publishedAt     INTEGER   -- 发布时间戳
  UNIQUE(worldPublishId, chapterId)
  INDEX(worldPublishId)
```

## 版本感知

```
t_gameSession:
  + worldPublishId  INTEGER   -- 关联 t_storyWorld_published.id
  + worldVersion    INTEGER   -- session 创建/升级时记录的 publishedVersion
```

注意：单版本覆盖下 `worldPublishId` 始终指向同一行（被覆盖更新），**不能用它判断版本**。版本感知靠数值比较 `session.worldVersion` vs `t_storyWorld_published.version`：

- 相同 -> 正常继续
- 不同 -> 说明作者已发布新版，且（因 runtime 读 published）该 session 下次编排将自动落到新版。前端据此在"聊过"面板提示用户（见"用户通知与操作"）。

## 单版本覆盖的取舍（诚实标注）

| 场景 | 方向2（单版本覆盖） | 方向1（多版本） |
|------|------------------|--------------|
| 作者改草稿期间，在运行的 session | ✅ 不受影响（读 published） | ✅ 不受影响 |
| 作者**发布新版**后，在运行的旧 session | ⚠️ 自动切到新版（动态更新，强制） | ✅ 停在旧版，可选升级 |
| 用户能否"保留旧版继续玩" | ❌ 不能（旧版已被覆盖，设计上放弃） | ✅ 能 |
| 进度对齐 | 确定性对齐（默认）+ 可选 AI 语义匹配 | 硬编码 / AI 迁移 |
| 保留历史版本/回滚 | ❌ 不支持 | ✅ 保留全部历史行 |

核心取舍：**放弃"老存档停在旧版"**，换取不维护多版本、不做全量存档迁移、不做 BLOB。意味着用户面对新版没有"我不升级"的选项--作者发布即强制升级。这是方向2 与方向1 的根本分界。若产品需要"用户可选不升级"，本质就是要多版本，应走方向1。

## 动态数据处理

切换到新版时，session `stateJson` 里的数据分三类。依据 [ai_agent_版本更新存档升级助手.md](../游玩业务_ai_agent/ai_agent_版本更新存档升级助手.md) 的结论：**绝大部分动态数据原样保留即可，真正需要处理的只是"结构引用"**。

| state 数据 | 处理方式 | 是否需 AI |
|-----------|---------|----------|
| `chapterProgress.phaseId` | 精确匹配新版 phase -> 保留；否则索引就近；再否则回退到章节首个 phase | 仅"改名匹配"可借 AI |
| `chapterProgress.eventIndex` / `completedEvents` | 跟随 phase 对齐；清理失效 phaseId 引用 | 否 |
| `dynamicEvents[].phaseId` | 跟随 phase 对齐 | 否 |
| `recentEvents[].phaseId` | 跟随 phase 对齐；失效的过滤 | 否 |
| `runtimeFacts` / `memoryFacts` | **原样保留**（用户经历，不丢） | 否 |
| `memorySummary` | **原样保留** | 否（AI 版可微调引用） |
| `flags` / `vars` | **原样保留**（孤儿 flag 不删，编排师自然忽略） | 否 |
| `npcs` / `inventory` / `unlockedRoles` | **原样保留**（孤儿 NPC 保留，不映射） | 否 |
| `player` / `narrator` | **原样保留**（运行时叠加值保留） | 否 |
| `turnState` | 对齐后重置为"等待玩家输入"安全态 | 否 |

**确定性对齐算法**（无需 AI，默认执行）：

1. 取旧 outline phase 列表（从 session 记录或旧 published）与新 published outline
2. `phaseId` 在新 outline 中存在 -> 直接用
3. 不存在 -> 按同名 -> 按相近索引就近 -> 都不行则回退到首个 phase，`eventIndex` 清零
4. `completedEvents` / `recentEvents` 里的失效 phaseId：能映射的映射，不能的丢弃
5. 其余动态数据（facts / flags / vars / npcs / memory）**一字不改**
6. 生成"对齐报告"：哪些 phase 被映射 / 回退 / 丢弃

## 发布后的进度对齐（确定性，非迁移）

单版本覆盖下没有"旧版 vs 新版并存"，所以不做方向1 的 `migrateSessionProgress` 全量迁移。上面"动态数据处理"的确定性对齐在 **session 下次进入编排时自动执行一次**（检测到 `worldVersion` 落后于 published.version），把 state 对齐到新 outline。对齐是幂等的、纯本地的、零 token 成本。对齐报告同时返回前端，作为"聊过"面板弹框的预览。

## 与 AI 存档升级助手的关系

**动态数据不是只能走 AI agent。** 上表已说明：只有一项--`phaseId` 的**改名语义匹配**--AI 能比确定性算法做得更好（作者把"苏醒"改成"入校"，确定性算法精确匹配失败后只能按索引兜底，AI 能语义对上）。其余全部确定性保留即可。

因此 AI agent 在方向2 里是**可选增强**，不是必需：

- **默认路径（零 AI）**：确定性对齐。覆盖"作者只改了文本 / 增删 phase"的绝大多数场景，对齐准确。
- **可选路径（借 AI）**：仅当确定性对齐报告里出现"phase 改名无法精确匹配"时，弹框提供"智能对齐"按钮，调用 agent 做 phase 语义匹配 + 重新生成 `eventSummary`。输入精简（phase 列表 + 当前进度，约 3200 token，约 ¥0.0005/session）。

agent 的完整输入/输出/prompt 规范见 [ai_agent_版本更新存档升级助手.md](../游玩业务_ai_agent/ai_agent_版本更新存档升级助手.md)。方向2 只复用其 `phaseMapping` + `newEventSummary` 子集；角色/背景迁移（`npcMapping` / `backgroundMigration`）在方向2 里用确定性保留替代，不必调用。

## 用户通知与操作（复用方向1"聊过"面板）

通知位置直接沿用方向1：**"聊过"面板 -> 对应故事块 -> "继续"按钮左侧 -> "故事已更新"按钮**（仅当 `session.worldVersion < published.version` 时显示）。

点击弹框：

```
┌─ 故事已更新到第 N 版 ──────────────────────┐
│ 作者发布了新版本，你的存档将切换到新版继续。 │
│                                            │
│ 进度对齐预览：                              │
│  • 阶段"苏醒" -> "入校"（已对齐）            │
│  • 阶段"选择装备"已移除（回退到"出发"）      │
│                                            │
│ [继续游玩]  [重新开始]  [智能对齐*]         │
│                                            │
│ * 仅当有 phase 改名无法精确匹配时出现        │
└────────────────────────────────────────────┘
```

三个操作：

1. **继续游玩**（默认）：执行确定性对齐，保留全部动态数据，落到新版继续。零成本。
2. **智能对齐**（条件出现）：在确定性对齐基础上，额外调 AI agent 做 phase 语义匹配 + 重新生成事件摘要。约 ¥0.0005。仅当对齐报告含"改名无法匹配"时显示。
3. **重新开始**：放弃当前进度，用新版从头开始（现有删档重开功能）。

**与方向1 的关键差异**：方向1 的弹框有"保留不处理（继续旧版）"选项，因为多版本下旧版还在；方向2 单版本覆盖下旧版已被覆盖，**没有此选项**--作者发布即强制升级，用户只能在"对齐继续 / 重开"间选。这是方向2 取舍的直接体现。

## 开发难度评估

| 阶段 | 改动范围 | 难度 | 工时估算 | 风险点 |
|------|---------|------|---------|--------|
| 版本字段 + 检测 | initDB 加字段、fixDB 迁移、publishWorldSynchronously 加 `version++`、listSession 返回版本对比、前端"故事已更新"按钮 | 低 | 1-2 天 | 低 |
| published 表 + 发布逻辑 | 新建 2 张表、`publishWorldSynchronously` 改为"草稿 -> 复制写 published（覆盖）"、删除故事级联清 published | 中 | 2-3 天 | 低（覆盖语义简单） |
| **runtime 读路径改造** | SessionService/TaskProgressEngine/TriggerEngine/SessionMemoryWorker 等 20+ 处 `db("t_storyChapter")` 改读 `t_storyChapter_published`；world.settings 读 published | 中 | 3-4 天 | 中（遗漏某处没改 -> 静默 bug） |
| 发布后进度对齐 | 确定性对齐函数 + 对齐报告 UI；可选 AI 语义匹配（复用 agent phaseMapping 子集） | 低-中 | 1-2 天 | 低 |

**总计** ≈ 7-11 天。比方向1 精简版（9-13 天）省 2-3 天，主要省在：无多版本累积逻辑、无全量存档迁移（仅确定性对齐）、无 BLOB/缓存层。AI 语义匹配作为可选增强，不进基础工时。

## 开发收益评估

| 收益项 | 价值 | 说明 |
|--------|------|------|
| 解决"作者编辑草稿静默影响在运行 session" | 🔴 高 | runtime 改读 published，草稿编辑彻底隔离（与方向1 同等收益） |
| 用户感知"故事已更新" | 🟡 中 | 体验改善 |
| 发布即动态更新（玩家拿最新版） | 🟡 中 | 单版本覆盖的直接收益，无需用户操作 |
| 支持版本回滚/历史 | ❌ 无 | 方向2 明确放弃 |
| 存档迁移平滑升级 | 🟡 中-低 | 确定性对齐默认执行（零成本）；AI 语义匹配按需可选（约¥0.0005/次），仅处理 phase 改名 |

## 总结

| 维度 | 评分 | 说明 |
|------|------|------|
| 方案合理性 | 8/10 | 自洽：单版本 + 完整快照（含 content）+ runtime 改读路径，无 outline/content 割裂；动态数据以确定性对齐为主、AI 为可选增强；取舍明确 |
| 开发难度 | 中 | 主工作量在 runtime 读路径改造（20+ 处），但每处都是机械替换 |
| 开发收益 | 中 | 核心收益（草稿/发布分离）与方向1 持平，放弃回滚/迁移换取更短工时 |
| 风险 | 低-中 | runtime 改造遗漏某处会静默 bug；覆盖语义下"发布即升级"需产品确认可接受 |
| ROI | 优 | 7-11 天拿到草稿/发布分离 + 版本感知 + 确定性进度对齐；AI 增强按需开启，不占基础工时 |


## 已实现方向2 
### 功能验证测试用例

> 覆盖本次实施的全部功能点：数据层（新表+新列）、发布快照、runtime 改读发布表、版本感知、确定性进度对齐、AI 智能对齐、前端通知与操作、删除级联、存量回填。每条标注「已自测 ✓」或「待自测」。
> 自测环境：`toonflow-app-run-db`（NODE_ENV=local，后端 `http://localhost:60002`），前端 `Toonflow-game-web`。

#### 一、数据层：新表 + 新列

| # | 用例 | 操作 / 预期 | 状态 |
|---|------|-----------|------|
| 1.1 | 新表创建（新环境） | 删库后启动 → `t_storyWorld_published`（含 UNIQUE(worldId)、idx_worldId）、`t_storyChapter_published`（含 UNIQUE(worldPublishId,chapterId)、triggersJson/tasksJson 列）存在 | 已自测 ✓（启动日志「创建数据表」） |
| 1.2 | 新表创建（老环境升级） | 已有数据的库启动 → fixDB `ensureTable` 幂等建两张表，不破坏既有数据 | 已自测 ✓（run-db 老库升级成功） |
| 1.3 | t_gameSession 新列 | `PRAGMA table_info(t_gameSession)` 含 `worldPublishId`、`worldVersion`（integer，可空） | 已自测 ✓ |
| 1.4 | database.d.ts 自动生成 | dev 启动后 `src/types/database.d.ts` 出现 `t_storyWorld_published`/`t_storyChapter_published` 接口与两新列；`DB` 聚合类型新增两表 key | 已自测 ✓（tsc 无类型错误） |

#### 二、发布快照（snapshotWorldToPublished）

| # | 用例 | 操作 / 预期 | 状态 |
|---|------|-----------|------|
| 2.1 | 首次发布生成快照 | 对已 published 的 world 调 `snapshotWorldToPublished` → `t_storyWorld_published` 出现一行（worldId 匹配，version=1），`t_storyChapter_published` 每章一行（content/runtimeOutline/title/sort + triggersJson/tasksJson） | 已自测 ✓（world 2 → 5 章；world 38 → 4 章） |
| 2.2 | 覆盖发布 version+1 | 同一 world 再次发布 → published 行 id 不变（=worldId），version=2；章节行先删后插（UNIQUE(worldPublishId,chapterId) 不冲突） | 已自测 ✓（world 2 v1→v2；world 38 v1→v2） |
| 2.3 | world.settings 完整快照 | published world 的 settings 含 `chapterInitialSnapshots`（开局快照），与草稿表一致 | 已自测 ✓（loadPublishedWorld 返回 settings 为 object） |
| 2.4 | trigger/task 快照 | published 章节行的 triggersJson/tasksJson = 该章 `t_chapterTrigger`/`t_chapterTask` 行数组（按 sort,id 排序）；无配置则为 `[]` | 已自测 ✓（world 2 无配置 → 0；world 38 同） |
| 2.5 | 发布失败不写 published | 发布过程抛错 → published 表不变（草稿表标 publish_failed） | 待自测（代码路径：catch 内不调 snapshotWorldToPublished） |
| 2.6 | settings 序列化 | enrichedWorld.settings 为对象时 → toJsonText 序列化存入；为字符串时直接存 | 已自测 ✓（worldPublish.ts typeof 分支） |

#### 三、runtime 改读发布表

| # | 用例 | 操作 / 预期 | 状态 |
|---|------|-----------|------|
| 3.1 | loadSessionWorld 读 published | session.worldPublishId>0 → `loadSessionWorld` 返回 `t_storyWorld_published` 行（含 version）；=0/null → 回退 `t_storyWorld` 草稿 | 已自测 ✓ |
| 3.2 | chapter 读取切 published | SessionService 18 处 `db("t_storyChapter")` → `loadPublishedChapter(worldPublishId, chapterId)`；无 worldPublishId 回退草稿 | 已自测 ✓（grep 残留仅回退分支） |
| 3.3 | trigger/task 读取切 published | TriggerEngine/TaskProgressEngine 读 `loadPublishedChapterTriggers/Tasks`；published 全量取后 JS 过滤 enabled=1（等价草稿 SQL where enabled=1） | 已自测 ✓（enabled 过滤逻辑） |
| 3.4 | 作者编辑草稿不影响运行 session | 作者改草稿 chapter content → 运行中 session 编排仍读 published 旧快照（隔离生效） | 待自测（需作者编辑场景） |
| 3.5 | 旧 session 兼容（无 worldPublishId） | worldPublishId 为空的旧 session → runtime 全部回退草稿表，不崩 | 已自测 ✓（未回填的 6 个 session） |
| 3.6 | 门面幂等归一化 | `loadPublishedChapter` 内部调 `normalizeChapterOutput`；调用方不再二次包裹（无双重归一化） | 已自测 ✓（review 确认） |
| 3.7 | 玩家入口路由切 published | startSession/introduction(正式游玩)/streamlines(sessionId分支)/storyInfo/getSession 玩家侧读 published；作者调试分支留草稿 | 已自测 ✓ |
| 3.8 | startSession 写版本字段 | 新建 session payload 含 `worldPublishId`+`worldVersion`（=published.version） | 已自测 ✓（字段已加） |

#### 四、版本感知

| # | 用例 | 操作 / 预期 | 状态 |
|---|------|-----------|------|
| 4.1 | 版本一致不触发对齐 | session.worldVersion == published.version → `addSessionMessageInner` 不执行对齐 | 已自测 ✓ |
| 4.2 | 版本落后触发确定性对齐 | session.worldVersion < published.version → 编排入口执行 `alignSessionProgress`，写回 worldVersion + stateJson + alignReport | 已自测 ✓ |
| 4.3 | 新 session 不误触发 | startSession 时 worldVersion=published.version → 首次编排不对齐 | 已自测 ✓ |
| 4.4 | 旧 session（worldVersion=0）不触发 | worldVersion 为 0（未回填）→ 不对齐，走草稿回退 | 已自测 ✓（条件 `>0`） |
| 4.5 | 对齐幂等 | 已对齐 session 再次编排 → worldVersion 已=published，不重复对齐 | 已自测 ✓ |

#### 五、确定性进度对齐（progressAlign）

| # | 用例 | 操作 / 预期 | 状态 |
|---|------|-----------|------|
| 5.1 | phase 精确匹配 | 旧 phaseId 在新 outline 存在 → reason=exact，phaseId 保留 | 已自测 ✓（world 38 全 exact） |
| 5.2 | phase 索引兜底 | 旧 phaseId 不在新版 → 索引就近映射，reason=nearby，hasUnmatchedRename=true | 已自测 ✓（构造 phase_0_old） |
| 5.3 | completedEvents 映射 | `phase:旧id` → `phase:新id`；无法映射的丢弃并记入 dropped | 已自测 ✓ |
| 5.4 | recentEvents 过滤 | 失效 phaseId 的 recentEvent 过滤；能映射的更新 | 已自测 ✓ |
| 5.5 | dynamicEvents 跟随 | dynamicEvents[].phaseId 跟随映射；无映射的保留原值 | 已自测 ✓ |
| 5.6 | 动态数据原样保留 | flags/vars/npcs/inventory/memorySummary/memoryFacts/runtimeFacts 一字不改 | 已自测 ✓（flags.questA/vars.gold/npcs.李四/memorySummary 全保留） |
| 5.7 | turnState 重置 | 对齐后 turnState = {canPlayerSpeak:true, expectedRoleType:player, expectedRole:玩家名} | 已自测 ✓ |
| 5.8 | alignReport 切章清理 | 切章时 `resetSessionChapterRuntimeOnSwitch` 删 state.alignReport，不残留错章节报告 | 代码确认 ✓（`delete state.alignReport` 已加） |
| 5.9 | eventIndex 回退 | 非精确映射时 eventIndex 清零（回退首 phase） | 已自测 ✓ |

#### 六、AI 智能对齐（StoryUpdateAlignAgent）

| # | 用例 | 操作 / 预期 | 状态 |
|---|------|-----------|------|
| 6.1 | 无改名不调 AI | hasUnmatchedRename=false → 直接确定性对齐落库，source=deterministic，不调 LLM | 已自测 ✓ |
| 6.2 | 有改名调 AI | hasUnmatchedRename=true → 调 `runStoryUpdateAlignAgent`（storyMemoryModel）做语义匹配 | 已自测 ✓（latencyMs=3868） |
| 6.3 | AI 语义匹配覆盖 nearby | AI 返回 phaseMapping → base.mapped 的 reason=nearby 被替换为 reason=name | 已自测 ✓ |
| 6.4 | AI 重新生成 eventSummary | AI 返回 newEventSummary -> 写入 chapterProgress.eventSummary | 已自测 ✓（旧 eventSummary -> AI 用新版语境重生成，语义保留） |
| 6.5 | AI newPhaseId 应用 | AI 返回 newPhaseId 在新 outline 中 → 更新 chapterProgress.phaseId/phaseIndex | 已自测 ✓（phase_0_旧版 → phase_1_暗流） |
| 6.6 | AI 失败兜底 | AI 调用异常/JSON 解析失败/schema 校验失败 -> fallback 确定性对齐，source=deterministic，warnings 含「AI 失败」 | 代码确认 ✓（fallback 函数与 PlayTipAgent 同款模式，try/catch + jsonMatch + safeParse 三层兜底） |
| 6.7 | 已对齐不重复调 | session.worldVersion >= published → alignSessionWithAi 返回 source=noop，不调 AI | 已自测 ✓ |
| 6.8 | 模型配置 | agent 用 `storyMemoryModel`（与记忆管理器同款），非 storyOrchestratorModel | 已自测 ✓ |
| 6.9 | prompt 从 DB 读取 | `loadTaskPrompt("story-update-align-agent")` 优先 customValue > defaultValue > FALLBACK_SYSTEM；DB 有该行（id=50） | 已自测 ✓ |
| 6.10 | dynamicEvents/completedEvents 跟随 AI 映射 | AI phaseMapping 应用到 dynamicEvents[].phaseId 与 completedEvents | 已自测 ✓（代码 merge 逻辑） |

#### 七、前端通知与操作（SceneHistory.vue「聊过」面板）

| # | 用例 | 操作 / 预期 | 状态 |
|---|------|-----------|------|
| 7.1 | storyUpdated 按钮显示 | session.storyUpdated=true → 「继续」按钮左侧显示橙色「故事已更新」按钮；=false 不显示 | 已自测 ✓（listSession 返回字段） |
| 7.2 | 弹框对齐预览 | 点击按钮 → 弹框展示 alignReport 摘要（mapped/fallback/dropped 转中文） | 待自测（需前端 dev server） |
| 7.3 | 继续游玩 | 点「继续游玩」→ 调 continueSessionForWorld，后端编排入口自动对齐 | 待自测（前端交互） |
| 7.4 | 智能对齐按钮条件显示 | alignReport.hasUnmatchedRename=true → 显示「智能对齐*」按钮；否则不显示 | 待自测（v-if 逻辑已实现） |
| 7.5 | 智能对齐调用 | 点「智能对齐」→ 调 `/game/alignSession` → 成功后 continueSessionForWorld | 待自测（前端调 store.alignSessionWithAi） |
| 7.6 | 重新开始 | 点「重新开始」→ deleteSession + startFromWorld | 待自测（前端交互） |
| 7.7 | 无「保留旧版」选项 | 弹框无「保留不处理（继续旧版）」按钮（方向2 单版本覆盖，与方向1 差异） | 已自测 ✓（代码无此按钮） |
| 7.8 | 加载态/禁用 | 处理中按钮 disabled，文案「处理中...」 | 待自测（代码已实现） |

#### 八、删除级联 + 存量回填

| # | 用例 | 操作 / 预期 | 状态 |
|---|------|-----------|------|
| 8.1 | 删世界清 published | deleteWorld 事务内删 t_storyChapter_published(worldPublishId=worldId) + t_storyWorld_published(worldId=worldId) | 代码确认 ✓（deleteWorld.ts 已加两行 delete，review 确认逻辑） |
| 8.2 | 回填已发布 world 的 session | fixDB 启动 → worldPublishId 为空且 world 有 published 行的 session 被回填 worldPublishId+worldVersion | 已自测 ✓（world 2 session 回填） |
| 8.3 | 回填跳过无 published 的 world | world 无 published 行的 session 不回填（worldPublishId 保持 null，走草稿回退） | 已自测 ✓（6 个 session 跳过） |
| 8.4 | 回填幂等 | 已回填的 session（worldPublishId>0）重启不被重复更新 | 已自测 ✓（条件 whereNull/0） |
| 8.5 | 回填不误标版本 | 回填设 worldVersion=当前 published.version；world 重新发布后回填不动已回填 session（version 差留给对齐处理） | 已自测 ✓ |

#### 九、回归与边界

| # | 用例 | 操作 / 预期 | 状态 |
|---|------|-----------|------|
| 9.1 | tsc 无新增错误 | 全量 `tsc --noEmit` 错误数 = 实施前基线（23），改动文件零新增 | 已自测 ✓ |
| 9.2 | 前端构建 | `vite build` 成功，SceneHistory.vue 打包进 dist | 已自测 ✓ |
| 9.3 | 前端 vue-tsc | SceneHistory.vue + toonflow.ts 零类型错误（worldIntro 补字段） | 已自测 ✓ |
| 9.4 | published.id=worldId 一致性 | 快照/读取/删除均依赖 published.id=worldId（单版本），全链路一致 | 已自测 ✓（review 确认） |
| 9.5 | loadPublishedWorld 按 worldId 查 | 门面按 worldId 列查（非 id 列），鲁棒不依赖 id=worldId 假设 | 已自测 ✓ |
| 9.6 | 作者调试链路不受影响 | orchestration debug / saveChapter / getChapter / debugStep / copyWorld 仍读草稿表 | 已自测 ✓（grep 确认未误改） |
| 9.7 | 旧 session 全链路不崩 | 无 worldPublishId 的旧 session：startSession/introduction/getSession/orchestration 全走草稿回退 | 已自测 ✓ |
| 9.8 | AI token 成本 | 实测 inputTokens~1212-1231 / outputTokens~194-234 / totalTokens~1425-1446（minimax MiniMax-M3，比设计估 3200 更低），aiTokenUsageLog 完整记录 type=存档智能对齐 | 已自测 ✓ |

### 待自测项汇总（优先级排序）

> 已完成：5.8、6.4、6.6、8.1、9.8（见上表「已自测 ✓」「代码确认 ✓」）.

1. **2.5** 发布失败不写 published -- 构造发布过程异常（如 AI 角色卡生成失败），确认 published 表不变
2. **3.4** 作者编辑草稿隔离 -- 作者改章节正文后，运行中 session 编排内容不变（需作者编辑场景）
3. **7.2-7.8** 前端弹框交互 -- 启前端 dev server（Toonflow-game-web，默认连 60002），浏览器实际点一遍「故事已更新」按钮 + 弹框三操作

### 自测脚本位置（已用，可复跑）

- 发布快照+门面读取：`/tmp/test-snapshot.ts`（worldId 参数化）
- 回填+对齐单元：`/tmp/test-backfill-align.ts`
- 端到端发布+回填：`/tmp/e2e-publish.ts`（worldId 参数化）
- HTTP 端到端：`curl /game/listSession` + `/game/alignSession`（token 由 `jwt.sign({id}, tokenKey)` 生成，tokenKey 取自 `t_setting`）
