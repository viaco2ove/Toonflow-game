# 记忆管理设计

## 1. 概述

记忆管理（Memory Management）是 AI 故事系统的长期记忆机制，通过分级管理确保大模型上下文不超限，同时保留重要信息。

## 2. 三级记忆系统

### 2.1 短期记忆（Short-term Memory）

**内容**：上下文装载最近聊天记录

**目的**：
- 适合容量限制
- 保留最近对话的完整性

**更新策略**：
- 每轮更新
- 按轮次追加

### 2.2 中期记忆（Mid-term Memory）

**内容**：上下文装载重要的事件和内容

**目的**：
- 避免遗忘重要的东西
- 保持上下文简洁

**更新策略**：按事件更新（详见 3.1）

### 2.3 长期记忆（Long-term Memory）

**内容**：上下文装载故事背景等全局控制情报

**目的**：
- 提供稳定的世界观信息
- 避免每轮重复传递

**更新策略**：
- 固定不变
- 通过记忆管理 agent 更新

## 3. 记忆更新机制

### 3.1 触发条件

只有命中关键变化时才触发记忆更新：

1. **新关系建立/破裂**
2. **用户做出明确立场选择**
3. **新任务出现**
4. **新关键道具出现/消失**
5. **章节阶段切换**
6. **章节成功/失败**
7. **关键角色状态变化**
8. **重要剧情节点通过**

### 3.2 不触发更新的情况

- 普通插科打诨
- 日常对话
- 环境描述
- 非关键角色互动

### 3.3 记忆管理 Agent

记忆管理 Agent 负责：
1. 挖掘聊天内容
2. 生成记忆目录（索引）
3. 提取关键事实
4. 维护关系变化

```typescript
interface MemorySummary {
  recentFacts: string[];        // 最近事实
  keyRelationships: string[];   // 关键关系
  importantItems: string[];     // 重要道具
  pendingTasks: string[];       // 待办任务
  criticalFlags: string[];       // 关键标记
}
```

## 4. 动态缓存接口

```typescript
type SessionDynamicDigest = {
  currentPhaseId: string;
  currentConflict: string;
  currentGoal: string;
  keyFacts: string[];
  relationChanges: string[];
  taskChanges: string[];
  itemChanges: string[];
  visibleFocusRoles: string[];
  updatedAt: number;
};
```

## 5. 异步记忆刷新

### 5.1 原则

首屏返回不要等记忆管理完成。

### 5.2 流程

```
发言成功后
    ↓
先提交消息
    ↓
后台排队做记忆更新
    ↓
成功后静默更新 memorySummary
```

## 6. 记忆摘要生成

### 6.1 事件摘要状态

```typescript
interface RuntimeEventDigestState {
  eventId: string;
  dynamicEvent: RuntimeDynamicEventState;
  memoryData: {
    recentFacts: string[];
    keyRelationships: string[];
    importantItems: string[];
  };
}
```

### 6.2 摘要生成时机

- 每个动态事件完成后
- 章节切换时
- 关键触发器命中时

## 7. 会话记忆工作器

SessionMemoryWorker 负责后台异步记忆刷新：

```typescript
// 每30秒轮询活跃会话，触发记忆更新
startSessionMemoryWorker();
```
- 后台异步线程：每30秒轮询活跃会话：读取该 session 最近 20 条消息（倒序） 对比 lastProcessedMessageId，有新消息才处理：refreshStoryMemoryBestEffort
- 意图分析触发：意图分析器认为是记忆管理需求是，例如@记忆管理 xxxx
- 编排师：编排师返回 trigger_memory_agent: true
- 关键节点触发（事件/章节生命周期）
 Toonflow Game 记忆管理 agent 的触发机制。

**总共 4 种触发路径**，按"谁/什么来触发"区分：

### 1️⃣ 编排师判定触发（语义触发，最核心）
- 位置：`fixDB.prompts.ts` 编排师 prompt
- 流程：编排师每轮编排时判断是否"有新信息/变化"（等级、物品、技能、关系、立场等），有就返回 `trigger_memory_agent: true`
- 落点：`SessionService.ts` 看到标志位就调 `refreshStoryMemoryBestEffort`
- 提示词里明确列了 4 个判定点：① 有新信息/变化 ② 用户状态变化（等级/物品/技能） ③ 用户输入 `@记忆管理` ④ 旁白输入 `@记忆管理`

### 2️⃣ 显式指令触发（`@记忆管理`）
- 位置：`PlayerMemoryDirectiveService.ts` + `SessionService.ts` 行 2434
- 流程：用户/旁白输入以 `@记忆管理` 开头的文本（如 `@记忆管理 睡觉恢复`）
- 优先级最高，**不依赖 AI 自由发挥**，直接同步写回用户参数卡并触发记忆刷新
- 配套日志：`story:memory_directive:stats`

### 3️⃣ 后台轮询触发（SessionMemoryWorker）
- 位置：`SessionMemoryWorker.ts`
- 流程：每 **30 秒**扫描活跃 session，对比 `lastProcessedMessageId`，有新消息就拉最近 20 条，调 `refreshStoryMemoryBestEffort`
- 失败退避：失败时 60s 重试
- 活跃状态过滤：仅 `active` / `chapter_completed`
- 异步执行，**首屏不等记忆完成**（设计原则）

### 4️⃣ 关键节点触发（事件/章节生命周期）
- 文档依据：`记忆管理.md` §6.2
- 触发时机：
  - 每个动态事件完成后
  - 章节切换时
  - 关键触发器命中时（`TriggerEngine`）
  - 章节成功/失败
- 实现路径：`ChapterProgressEngine` / `TriggerEngine` 调 `refreshStoryMemoryBestEffort`

---

**简单记**：

| # | 触发方 | 频率 | 关键文件 |
|---|---|---|---|
| 1 | 编排师（AI 判定） | 每轮 | `fixDB.prompts.ts` + `SessionService.ts` |
| 2 | `@记忆管理` 指令 | 不定 | `PlayerMemoryDirectiveService.ts` |
| 3 | SessionMemoryWorker | 30s 轮询 | `SessionMemoryWorker.ts` |
| 4 | 章节/事件/触发器 | 节点性 | `ChapterProgressEngine` / `TriggerEngine` |

如果按"自动 vs 显式"粗分，那就是 **3 种自动**（编排师、轮询、节点）+ **1 种显式**（`@记忆管理`）。_



## 8. 与其他模块的关系

- **NarrativeOrchestrator**：编排时注入记忆提示
- **ChapterProgressEngine**：阶段切换触发记忆更新
- **TriggerEngine**：触发器命中可能触发记忆更新
- **SessionService**：协调记忆刷新时机