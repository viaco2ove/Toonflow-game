# ai_agent:意图分析师
[任务系统设计.md](../%E5%B0%8F%E6%B8%B8%E6%88%8F%E8%AE%BE%E8%AE%A1/%E4%BB%BB%E5%8A%A1%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1.md)

## 可用模型
[rc/utils/ai/text/modelList.ts](rc/utils/ai/text/modelList.ts)
### 供应商：
local文本模型
火山引擎
DeepSeek
OpenAI
Gemini
t8star

### 模型
线上供应商：略。 
#### local文本模型
Qwen3-0.6B

## 本地大模型
本地大模型暂时只对接 Qwen3-0.6B。注意是 local文本模型 里暂时只有Qwen3-0.6B！而不是整个ai agent 只用Qwen3-0.6B
## 供应商是：
### 供应商：
local文本模型
火山引擎
DeepSeek
OpenAI
Gemini
t8star

### 
|模型|参数量|模型类型| 适用场景               |推荐理由
|---|---|---|--------------------|---|
|Qwen3-0.6B|0.6B|文本生成| （任务/剧情/系统命令）       | 轻量级模型，适合本地 CPU    |
|moka-ai/m3e-small|300M|嵌入匹配| 基础意图分类（任务/剧情/系统命令） |轻量级模型，适合本地 CPU|
|Qwen2.5-1.5B|1.5B|文本生成| 基础意图分类（任务/剧情/系统命令） | 极快，本地 CPU 也能跑    |
|Qwen2.5-3B|3B|文本生成| 中等复杂度意图 + 槽位提取     | 平衡速度和精度           |
|Qwen2.5-7B|7B|文本生成| 复杂意图推理、多意图分离       | 需要较强本地 GPU         |


### Qwen3-0.6B
https://www.modelscope.cn/models/Qwen/Qwen3-0.6B
git clone https://www.modelscope.cn/Qwen/Qwen3-0.6B.git
node-llama-cpp 方式自动安装（参考 MOSS_TTS 和 MODNet）
const MODEL_FILE_NAME = "Qwen3-0.6B-Q8_0.gguf";                                                                                                                           
// ModelScope（国内首选，速度快）                                                                                                                                         
const MODEL_MODELSCOPE_URL = "https://www.modelscope.cn/models/Qwen/Qwen3-0.6B-GGUF/resolve/master/Qwen3-0.6B-Q8_0.gguf";                                                 
// HuggingFace 镜像（国内可访问）                                                                                                                                         
const MODEL_MIRROR_URL = "https://hf-mirror.com/Qwen/Qwen3-0.6B-GGUF/resolve/main/Qwen3-0.6B-Q8_0.gguf";                                                                  
// HuggingFace 官方                                                                                                                                                       
const MODEL_DOWNLOAD_URL = "https://huggingface.co/Qwen/Qwen3-0.6B-GGUF/resolve/main/Qwen3-0.6B-Q8_0.gguf";            

### moka-ai/m3e-small
暂时不对接moka-ai/m3e-small 
moka-ai/m3e-small 是**文本嵌入模型（Embedding Model）**，不是生成式模型（Instruct/Chat Model）。
m3e-small 不能直接用来做意图分析——它不会输出"创建任务"这样的标签。但可以用间接方式实现意图分类：

方案：嵌入 + 向量相似度匹配
用户输入 → m3e-small 编码 → 768维向量
                              ↓
                    与预定义意图向量计算余弦相似度
                              ↓
                    取最高相似度的意图作为分类结果

优点
极轻量：24M 参数，推理几乎无延迟，CPU 就能跑
确定性：不像生成模型可能输出格式不稳定
适合的场景：意图类别有限（创建/退出/查询/正常对话），不需要复杂推理
缺点
需要手动维护意图描述文本库
对复杂/模糊意图区分能力弱
新增意图需要重新编码向量

| | m3e-small (嵌入匹配) | Qwen2.5-1.5B (生成分类)          |
|---|---|------------------------------|
| 模型大小 | ~90MB | ~1.5GB                       |
| 推理速度 | 极快（<10ms） | 快（~100ms）                    |
| 模型类型 | 文本嵌入模型 (Embedding Model) | 生成式模型 (Instruct/Chat Model/Causal LLM) |
| 分类准确度 | 中等（简单场景够用） | 较高                           |
| 灵活性 | 低（需预定义意图） | 高（可处理未见意图）                   |
| 输出可控性 | 高（向量匹配确定性强） | 中（需约束输出格式）                   |
| 部署复杂度 | 低 | 中                            |



## 其他文本大模型
复用已对接的文本模型。

## 模型配置
SceneSettings.vue

## ai_agent 提示词
SceneSettings.vue
[fixDB.ts](../../../src/lib/fixDB.ts)
[initDB.ts](../../../src/lib/initDB.ts)
[意图分析师_prompt.md](%E6%84%8F%E5%9B%BE%E5%88%86%E6%9E%90%E5%B8%88_prompt.md)


## 测试
● 意图分析师集成情况 + 测试方案 ✅

  ✅ 已嵌入流程（确认）

  MiniGameController.handleMiniGameTurn（每轮用户输入都经过）：

  用户消息 → SessionService.addMessage
              ↓
         handleMiniGameTurn (L5632)
              ↓
         ┌── Fast Path: # 命令 ──┐  ← T1.3 已嵌入
         │   analyzeIntent()      │
         │   ✅ create_task       │
         │   ⏳ exit_task (T4)    │
         │   ⏳ query_progress(T4)│
         └────────────────────────┘
              ↓ (未命中)
         ┌── Slow Path: AI 分类 ──┐  ← T3.3 已嵌入
         │ analyzeIntentWithAi    │
         │   Fallback()           │
         │ → Qwen3-0.6B 推理      │
         │   置信度 ≥ 0.7        │
         │ ✅ create_task         │
         │ ⏳ exit_task (T4)      │
         │ ⏳ query_progress (T4) │
         │ → 其他继续主线         │
         └────────────────────────┘

  🧪 3 种测试方法

  方法 1：端到端测试脚本（推荐 - 真实调用 Qwen3）

  cd toonflow-game-app
  npx tsx test/testcase/intent-analyzer.qwen060-e2e.test.ts

  测试 9 个场景：
  - 3 个命令快路径（不调 AI，秒级）
  - 6 个 AI 自然语言识别（调 Qwen3）：
    - 好，我接受这个挑战 → create_task
    - 算了，我不想做了 → exit_task
    - 我的任务进度怎么样了 → query_progress
    - 我攻击那只哥布林 → game_action
    - 老板，今天生意怎么样 → normal_dialog
    - 我想去探索古墓 → create_task（核心场景）
    - 要求旁白推荐任务。用户接受任务试试。用户提出自己定制的任务试试。

  方法 2：通过游戏界面实测

  1. 设置：设置页 → 模型配置 → 意图分析师 → 配置 qwen060/qwen3-0.6b
  2. 进入游戏会话
  3. 测试输入（看后端控制台日志）：

  ┌────────────────────┬──────────────────────────────┬───────────────────────────┐
  │        输入        │           预期日志           │         预期效果          │
  ├────────────────────┼──────────────────────────────┼───────────────────────────┤
  │ #任务:打哥布林     │ [intent] 命令命中            │ 立即创建任务（fast path） │
  ├────────────────────┼──────────────────────────────┼───────────────────────────┤
  │ 好，我接受这个挑战 │ [intent] AI 分类 create_task │ 创建任务（slow path）     │
  ├────────────────────┼──────────────────────────────┼───────────────────────────┤
  │ 算了，我不想做了   │ [intent] AI 分类 exit_task   │ 仅打印日志（T4 待实现）   │
  ├────────────────────┼──────────────────────────────┼───────────────────────────┤
  │ 老板你好           │ （无意图日志）               │ 走原有剧情编排            │
  └────────────────────┴──────────────────────────────┴───────────────────────────┘

  方法 3：直接看后端日志

  启动后端时打开 debug 日志，每轮用户输入会输出：
  [intent] 命令命中 { intent: "create_task", ... }    ← 快路径
  [intent] AI 分类 create_task { confidence: 0.85 }   ← AI 路径
  [qwen3-0.6b] [inference] 推理完成，耗时 800ms        ← Qwen3 调用

  📋 当前限制

  ┌────────────────┬───────────┬───────────┬─────────────┐
  │      意图      │ 命令路径  │  AI 路径  │    状态     │
  ├────────────────┼───────────┼───────────┼─────────────┤
  │ create_task    │ ✅ 完整   │ ✅ 完整   │ 可用        │
  ├────────────────┼───────────┼───────────┼─────────────┤
  │ exit_task      │ 🟡 仅日志 │ 🟡 仅日志 │ T4.x 待实现 │
  ├────────────────┼───────────┼───────────┼─────────────┤
  │ query_progress │ 🟡 仅日志 │ 🟡 仅日志 │ T4.x 待实现 │
  ├────────────────┼───────────┼───────────┼─────────────┤
  │ game_action    │ -         │ 🟡 走原有 │ 不需新逻辑  │
  ├────────────────┼───────────┼───────────┼─────────────┤
  │ normal_dialog  │ -         │ ✅ 走原有 │ 不需新逻辑  │
  └────────────────┴───────────┴───────────┴─────────────┘
